# ============================================
# Client Environment Variables
# ============================================
# Copy this file to .env.local or .env and fill in your actual values
# Vite requires the VITE_ prefix for environment variables to be exposed to the client
#
# IMPORTANT: After changing this file, copy it to .env.local and update with your actual values
# Then restart the dev server (npm run dev) or rebuild (npm run build) for production

# ============================================
# Google OAuth Configuration (REQUIRED)
# ============================================
# Get these from: https://console.cloud.google.com/apis/credentials
# 
# Google OAuth Client ID (Web Application) - REQUIRED for Google Sign-In button
# This MUST match the GOOGLE_CLIENT_ID in server/.env
# Example: 202761390203-6pc1ievc2rv408tgouqkmmr4et083jt7.apps.googleusercontent.com
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ============================================
# API Configuration
# ============================================
# API Base URL for production builds
# 
# IMPORTANT: In production, this must be set correctly!
# 
# Option 1: Same server (server serves both static files and API)
#   - Use relative path: /api/auth
#   - This is the default if not set
#   - Example: VITE_API_BASE_URL=/api/auth
#
# Option 2: Separate API server
#   - Use full URL: https://api.yourdomain.com/api/auth
#   - Example: VITE_API_BASE_URL=https://api.yourdomain.com/api/auth
#
# For development: Leave as default (uses Vite proxy)
# For production: Set to /api/auth (same server) or full API URL (separate server)
VITE_API_BASE_URL=/api/auth

# ============================================
# Application Configuration
# ============================================
# Application Environment
# Options: development, production
VITE_APP_ENV=development

# Application Name
VITE_APP_NAME=MERN Starter

# Application Version
VITE_APP_VERSION=1.0.0

# ============================================
# Development Configuration
# ============================================
# Development Server Port (default: 3000)
# Only used in development mode
VITE_PORT=3000

# API Target URL for Vite Proxy (default: http://localhost:5000)
# Only used in development mode - Vite proxy configuration
# Only needed if different from default
VITE_API_TARGET=http://localhost:5000

# ============================================
# Production Configuration Notes
# ============================================
# IMPORTANT FOR PRODUCTION:
# 1. Set VITE_GOOGLE_CLIENT_ID with your actual Google Client ID
# 2. Set VITE_API_BASE_URL=/api/auth if server serves both static files and API
# 3. Set VITE_API_BASE_URL=https://your-api-domain.com/api/auth if using separate API server
# 4. Rebuild the client after changing environment variables:
#    cd client
#    npm run build
# 5. The built files in client/build will use these values
# 6. Environment variables are embedded at BUILD TIME, not runtime!

# ============================================
# Setup Instructions
# ============================================
# 1. Copy this file: cp env.example .env.local
# 2. Edit .env.local and set your actual Google Client ID
# 3. For development: Restart dev server (npm run dev)
# 4. For production: Rebuild (npm run build) after setting variables
#
# IMPORTANT: 
# - VITE_GOOGLE_CLIENT_ID must match GOOGLE_CLIENT_ID in server/.env
# - Never commit .env.local to version control
# - Always use .env.example as a template
# - Environment variables must start with VITE_ to be exposed to client code
# - Access variables in code: import.meta.env.VITE_VARIABLE_NAME
