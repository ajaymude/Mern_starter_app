# ============================================
# Client Environment Variables
# ============================================
# Copy this file to .env.local or .env and fill in your actual values
# Vite requires the VITE_ prefix for environment variables to be exposed to the client

# ============================================
# API Configuration
# ============================================
# API Base URL (optional - defaults to /api via Vite proxy)
# For production, set this to your backend URL
# Example: https://api.yourdomain.com
VITE_API_BASE_URL=/api

# ============================================
# Google OAuth Configuration
# ============================================
# Get these from: https://console.cloud.google.com/apis/credentials
# 
# Google OAuth Client ID (Web Application) - Required
# This is used for Google Sign-In button
# Get from: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ============================================
# Application Configuration
# ============================================
# Application Environment
# Options: development, production
VITE_APP_ENV=development

# Application Name
VITE_APP_NAME=MERN Starter

# Application Version
VITE_APP_VERSION=1.0.0

# ============================================
# Feature Flags (Optional)
# ============================================
# Enable/disable features
# VITE_ENABLE_ANALYTICS=false
# VITE_ENABLE_ERROR_TRACKING=false

# ============================================
# Development Configuration
# ============================================
# Development Server Port (default: 3000)
VITE_PORT=3000

# API Target URL for Vite Proxy (default: http://localhost:5000)
# Only needed if different from default
VITE_API_TARGET=http://localhost:5000

# ============================================
# Notes
# ============================================
# 1. Never commit .env.local or .env files to version control
# 2. Always use .env.example as a template
# 3. Restart Vite dev server after changing .env files
# 4. Environment variables must start with VITE_ to be exposed to client code
# 5. Access variables in code: import.meta.env.VITE_VARIABLE_NAME

